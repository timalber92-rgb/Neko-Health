# HealthGuard Frontend - STAGING Environment Configuration

# Backend API URL
# For local staging: use localhost
# For deployed staging: use your staging API URL
VITE_API_URL=http://localhost:8000

# API Key for authenticated requests
# This key must match one of the keys in backend/.env.staging
VITE_API_KEY=REDACTED_API_KEY

# ============================================================================
# STAGING ENVIRONMENT NOTES:
# ============================================================================
# To use this configuration:
# 1. Copy this file to .env: cp .env.staging .env
# 2. Restart the frontend dev server
#
# The frontend will automatically include the API key in all requests
# to the backend API endpoints.
#
# When deploying to a hosting service:
# - Update VITE_API_URL to your deployed backend URL
# - Keep VITE_API_KEY secure (use environment variables in your hosting platform)
# ============================================================================
