# HealthGuard Backend - PRODUCTION Environment Configuration
# IMPORTANT: DO NOT commit this file with real secrets to version control!

# API Configuration
API_TITLE=HealthGuard API
API_DESCRIPTION=Cardiovascular Disease Risk Prediction & Intervention Recommendation
API_VERSION=1.0.0
API_HOST=0.0.0.0
API_PORT=8000
DEBUG=false

# CORS Configuration
# CRITICAL: Replace with your actual frontend domain(s) in production
# DO NOT use wildcards (*) in production!
CORS_ORIGINS=https://your-frontend-domain.com,https://app.your-domain.com
CORS_ALLOW_CREDENTIALS=true
CORS_ALLOW_METHODS=GET,POST,OPTIONS
CORS_ALLOW_HEADERS=Content-Type,X-API-Key

# Security Configuration
# CRITICAL: Enable API key authentication in production!
API_KEY_ENABLED=true

# CRITICAL: Generate secure random API keys for production
# Use: python -c "import secrets; print(secrets.token_urlsafe(32))"
# Add multiple keys separated by commas for different clients
API_KEYS=REPLACE_WITH_SECURE_KEY_1,REPLACE_WITH_SECURE_KEY_2

# Rate Limiting (recommended for production)
RATE_LIMIT_ENABLED=true
RATE_LIMIT_REQUESTS=100

# Model Configuration (optional overrides)
# MODELS_DIR=./models
# RISK_PREDICTOR_FILENAME=risk_predictor.pkl
# INTERVENTION_AGENT_FILENAME=intervention_agent.pkl
# SCALER_PATH=./data/processed/scaler.pkl

# Logging
LOG_LEVEL=INFO
LOG_FORMAT=%(asctime)s - %(name)s - %(levelname)s - %(message)s

# Environment (DO NOT change this in production!)
ENVIRONMENT=production

# ============================================================================
# PRODUCTION CHECKLIST:
# ============================================================================
# [ ] Replace CORS_ORIGINS with your actual frontend domain(s)
# [ ] Generate and set secure API_KEYS (minimum 32 characters)
# [ ] Verify API_KEY_ENABLED=true
# [ ] Verify ENVIRONMENT=production
# [ ] Set appropriate RATE_LIMIT_REQUESTS for your use case
# [ ] Review and adjust LOG_LEVEL if needed
# [ ] Ensure this file has proper permissions (chmod 600)
# [ ] Never commit this file with real secrets to git
# ============================================================================
